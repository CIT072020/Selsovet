   При создании обновления исключать файл: ConfForms.ini

   Обращать внимание при создании инсталяции:
 
     1. Не забывать изменять значение для "version" и "update"
        (значения "update" также должны наращиваться. так как
         каждая новая версия включает в себя и update )

     2. При создании update не забывать менять IDUpdate и IDProg

     3. Помещать новые версии Service-файлов в папку ServiceNew
        включая при необходимости NewParams.tsk